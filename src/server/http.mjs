import http from 'node:http';import {spawn} from 'node:child_process';import {DATASET_DIR,DOWNLOAD_TOKEN,PORT} from '../config.mjs';
export function startHttp(statusFn){const server=http.createServer((req,res)=>{const u=new URL(req.url,`http://${req.headers.host||'localhost'}`);if(u.pathname==='/health'){res.setHeader('content-type','application/json');return res.end(JSON.stringify({ok:true,...statusFn()}));}if(u.pathname==='/status'){res.setHeader('content-type','application/json; charset=utf-8');return res.end(JSON.stringify(statusFn(),null,2));}if(u.pathname==='/download'){if(DOWNLOAD_TOKEN&&u.searchParams.get('token')!==DOWNLOAD_TOKEN){res.statusCode=403;return res.end('forbidden');}const s=statusFn();if(!s.runId){res.statusCode=404;return res.end('no dataset');}res.setHeader('content-type','application/gzip');res.setHeader('content-disposition',`attachment; filename="spot-depth-${s.runId}.tar.gz"`);const p=spawn('tar',['-czf','-','-C',DATASET_DIR,s.runId]);p.stdout.pipe(res);p.stderr.on('data',()=>{});p.on('close',c=>{if(c!==0&&!res.headersSent)res.statusCode=500;});return;}res.setHeader('content-type','text/html; charset=utf-8');const s=statusFn();res.end(`<!doctype html><meta charset=utf-8><title>現貨深度採集</title><style>body{font-family:system-ui;max-width:900px;margin:40px auto;padding:0 20px}pre{background:#111;color:#eee;padding:16px;overflow:auto}a{font-size:20px}</style><h1>聚合現貨深度資料採集</h1><p>狀態：<b>${s.status}</b></p><p>Run ID：${s.runId||'-'}</p><p><a href="/download${DOWNLOAD_TOKEN?`?token=${DOWNLOAD_TOKEN}`:''}">下載目前資料（串流 tar.gz）</a></p><pre>${escapeHtml(JSON.stringify(s,null,2))}</pre>`);});server.listen(PORT,'0.0.0.0');return server;}
function escapeHtml(s){return s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));}
