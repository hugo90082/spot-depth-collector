import test from 'node:test';import assert from 'node:assert/strict';import {OrderBook} from '../src/core/orderbook.mjs';import {PersistenceTracker} from '../src/core/persistence.mjs';import {encodeBlock} from '../src/storage/block-codec.mjs';import {BlockWriter} from '../src/storage/block-writer.mjs';
test('shell aggregation uses left-closed/right-open boundaries',()=>{const b=new OrderBook('x');b.replace([[99.5,2],[98,3]],[[100.5,4],[102,5]]);const s=b.shellQty([0,100,300]);assert.equal(s.mid,100);assert.equal(s.bid[0],2);assert.equal(s.ask[0],4);assert.equal(s.bid[1],3);assert.equal(s.ask[1],5);});
test('persistence reset on disappearance',()=>{const b=new OrderBook('x');b.replace([[99,2]],[[101,1]],0);const p=new PersistenceTracker();p.resetFromBook(b,0);assert.equal(p.persistentQty('bid',99,11000,10000),2);p.onUpdate('bid',99,0,12000);p.onUpdate('bid',99,5,13000);assert.equal(p.persistentQty('bid',99,20000,10000),0);});
test('persistence keeps quantity at window start when level increased',()=>{const b=new OrderBook('x');b.replace([[99,1]],[[101,1]],0);const p=new PersistenceTracker();p.resetFromBook(b,0);p.onUpdate('bid',99,5,5000);assert.equal(p.persistentQty('bid',99,12000,10000),1);});
test('block gzip and quality metadata',()=>{const meta={venue:'x',asset:'BTC',blockStartMs:1000};const r={type:'depth',kind:'near',actualTs:1100,mid:100,bid:new Array(14).fill(1),ask:new Array(14).fill(2),trustedBid:100,trustedAsk:100,observedBid:100,observedAsk:100,quality:'VALID'};const e=encodeBlock(meta,[r]);assert.ok(e.gzip.length>0);assert.equal(e.sha256.length,64);assert.match(e.raw.toString('utf8',12,300),/qualityCodes/);});
test('block writer retains adjacent blocks simultaneously',()=>{const w=new BlockWriter('x');w.add('v','BTC',{type:'reset',actualTs:299999});w.add('v','BTC',{type:'reset',actualTs:300000});assert.equal(w.states.size,2);});
